[project]
name = "base44-site-refatorado"
version = "0.1.0"
description = "Refatoracao HTML da landing page Base44 em Flask + Jinja2"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.0.0,<4.0.0",
]

[tool.uv]
package = false

